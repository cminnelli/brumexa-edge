{
  "name": "brumexa-edge",
  "version": "1.0.0",
  "description": "Brumexa-Edge — cliente de voz para LiveKit (Raspberry Pi / navegador)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "brumexa": "sudo kill -9 $(sudo ss -tlnp sport = :3000 2>/dev/null | grep -oE 'pid=[0-9]+' | grep -oE '[0-9]+') 2>/dev/null; nodemon server.js"
  },
  "dependencies": {
    "@livekit/rtc-node": "^0.13.25",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "livekit-server-sdk": "^2.6.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
